# AGENTS.md

- 了解上下文再动手: 修改前先检查相关文件、现有约定和最近改动
- 优先最小改动: 只改与当前任务直接相关的文件和代码路径
- 谨慎考虑添加新的依赖: 尽量减少打包后的文件大小, 自己实现是否性能更好
- 注释与文档有中文的, 必须使用英文半角标点符号, 非结尾的逗号/句号后添加一个空格 (就像这个文件一样)
- `Cargo`与`Rust`当前机器没有安装, 无法直接执行检测, 项目使用`Github Action`进行测试构建
- 任务完成后要询问用户是否要`push`代码, 用户确认后使用`gh run watch`跟踪日志输出, 修复错误与警告, 直到Action通过, 并给出继续优化的方向与建议**多选框**, 供用户选择使用方向键选择 (`gh`已在环境变量中, 可直接调用)